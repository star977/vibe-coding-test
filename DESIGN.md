# 网络资产测绘 CLI 设计文档

## 1. 这是什么（大白话版）

局域网里的设备——打印机、存储设备、电视盒子、智能音箱——有一个共同习惯：**它们会主动自我介绍**。
网络里有人问一句「这儿有谁、都提供什么服务」，它们就会回答自己的名字、提供哪些服务、
每个服务在哪个端口、甚至自己的型号和固件版本。

这个机制是设备出厂自带的，不需要密码也不需要登录，目的是让同网络的设备能自动发现彼此
（手机能自动找到打印机，靠的就是它）。

**本工具做的事**：给定一个地址范围和一个端口范围，挨个去问这些地址上的设备
「你是谁、你提供什么」，把所有回答整理成一份资产清单。

### 与端口扫描的本质区别

| | 端口扫描 | 本工具 |
|---|---|---|
| 方式 | 逐个端口敲门试探 | 直接问设备本人 |
| 端口从哪来 | 自己试出来的 | **设备自己报的** |
| 拿到的信息 | 「端口是开的」 | 名字、主机名、型号、固件、管理地址 |

**这决定了一个关键设计**：端口范围是**过滤条件**，不是扫描目标。见 §8.1。

## 2. 目标

做一个 Go 编写的命令行程序，对指定网段完成一次资产盘点，输出中每条资产至少包含：

- IP 地址（v4 / v6）
- 端口
- 主机名
- **深度标识信息**（型号、固件版本、服务路径等设备自报的键值对）

最后一项是核心目标，前三项是及格线。

## 3. 功能范围

### 做

1. 接收网段（CIDR）和端口范围两个输入
2. **单播通道**：并发探测网段内每个地址，逐一采集其自我介绍
3. **组播通道**：发起一次全网广播询问作为兜底，捕获只响应广播的设备
4. 解析出：服务类型、端口、实例名、主机名、v4/v6 地址、存活时间、**自报键值对**
5. 合并两通道结果，**用网段过滤组播所得**
6. 按端口范围过滤结果
7. 按规定格式输出
8. 超时控制，单个无响应地址不拖垮整体
9. 单元测试覆盖 §9 的验收项，尤其是真实环境难以构造的路径

### 不做（明确划出范围外）

- 不做端口探测 / 不发 TCP 连接
- 不做 JSON / CSV 等其他输出格式
- 不做结果持久化

## 4. 预期的功能流程

### 4.1 总流程

**总入参**

| 参数 | 形式 | 必填 | 默认 | 含义 |
|---|---|---|---|---|
| `--cidr` | `192.168.1.0/24` | 是 | — | 待测绘网段 |
| `--ports` | `1-10000` | 否 | `1-65535` | 端口**过滤**范围（非扫描范围，见 §8.1） |
| `--timeout` | `2s` | 否 | `2s` | **单个地址的总时间预算**（覆盖 Q1–Q3 全部往返，非单次等待） |
| `--concurrency` | `256` | 否 | `256` | 并发探测数，默认值使 /24 可一批打完。**硬上限 1024**，超出即截断并提示（§9.5-S5） |

**总出参**

| 通道 | 内容 |
|---|---|
| stdout | 资产清单文本，格式见 4.4 |
| stderr | 参数错误、运行统计（不污染 stdout） |
| 退出码 | `0` 正常（**含未发现任何资产**）／ `2` 参数非法 |

**端到端**

```text
--cidr 192.168.1.0/24 --ports 1-10000
              ↓
   ① → ② → ③ → ④ → ⑤ → ⑥
              ↓
         资产清单文本
```

### 4.2 节点总览

| 节点 | 入参 | 出参 |
|---|---|---|
| ① 参数解析 | 命令行参数 | `Config` |
| ② 目标生成 | `Config.Net` | 待探测地址**区间**（按需生成，不展开） |
| ③a 组播探测 | 超时 + 种子服务类型 | 全网响应设备的原始响应集合 |
| ③b 单播探测 | 地址区间 + 超时 + 并发数 | 各地址的原始响应集合 |
| ④ 响应解析 | 单地址的原始响应集合 | 该地址的 `Host` 半成品 |
| ⑤ 聚合过滤 | 全部 `Host` 半成品 + 端口范围 | 最终 `Host` 列表 |
| ⑥ 渲染输出 | `Host` 列表 | 文本 |

### 4.3 逐节点规格

#### 节点 ① 参数解析

| | |
|---|---|
| **入参** | 命令行原始参数 |
| **处理** | 1. 解析四个参数<br>2. 校验网段可解析<br>3. 校验端口范围：下界 ≤ 上界，且均在 1–65535<br>4. 校验超时 > 0、并发数 > 0 |
| **出参** | `Config{ Net, PortMin, PortMax, Timeout, Concurrency }` |
| **失败** | 任一校验不过 → stderr 打印**具体哪个参数、为什么不合法** → 退出码 2，**不得 panic** |

#### 节点 ② 目标生成

| | |
|---|---|
| **入参** | `Config.Net` |
| **处理** | 遍历网段内全部地址，剔除网络地址与广播地址 |
| **出参** | 地址区间 `ipRange`（仅记首末两个 32 位整数，恒 8 字节） |
| **边界** | `/32` → 返回该地址本身；`/31` → 返回两个地址（点对点网段无网络/广播地址概念） |
| **失败** | §9.3-B4：不得一次性展开进内存。实测展开成切片时 /12 占 29.4 MB、/8 约 470 MB，故改为按需逐个产出 |

#### 节点 ③a 组播探测（兜底通道）

| | |
|---|---|
| **入参** | 超时 |
| **处理** | 向本地链路的组播组发出一次询问，在超时窗口内持续收包。**不针对特定地址**，因此能捕获那些只监听组播、不响应单播询问的精简实现设备。<br>此外**补发一组常见服务类型的查询**：并非所有实现都响应服务类型元查询，这类设备若只被问元查询会整台漏报。仅在本通道补发——组播整轮只跑一次，而单播要按地址逐个发，查询量会随网段规模放大 |
| **出参** | `[]{ 响应源 IP, 原始响应报文集合 }` |
| **失败** | 组播套接字创建失败或接口不支持 → **降级为纯单播**，stderr 提示，**不中断整体** |
| **边界** | 会收到 `--cidr` 范围之外设备的回答，**必须在节点⑤用网段过滤** |
| **与 ③b 关系** | 两通道**并行**执行，结果在节点⑤合并去重 |

#### 节点 ③b 单播探测（主通道）

| | |
|---|---|
| **入参** | 地址列表、超时、并发数 |
| **处理** | 固定大小的工作池，每个地址独立一条协程：<br>**Q1** 问「你有哪些服务类型」<br>**Q2** 对返回的每个服务类型问「有哪些实例」<br>**Q3** 对每个实例补问缺失的端口/主机名/地址/描述<br>**关键**：Q2 的回答附加区通常已带齐 Q3 所需内容，应先榨干附加区，缺什么才发 Q3（见 §8.3） |
| **出参** | `[]{ IP, 原始响应报文集合 }` |
| **失败** | 单地址超时或不可达 → **静默跳过**，不打印、不中断其他地址 |
| **边界** | 同一地址可能返回多个报文，必须全部收集直到超时，不能收到第一个就停 |

#### 节点 ④ 响应解析

| | |
|---|---|
| **入参** | 单个地址的原始响应报文集合 |
| **处理** | 遍历全部响应区段（回答区 / 附加区 / 授权区），按记录类型分派：<br>· 服务类型清单 → `PTRTypes`<br>· 实例名 → `Service.Name`（**须做转义还原**）<br>· 定位记录 → `Port` / `Proto` / `Hostname`<br>· 描述记录 → `TXT`（**保序切片**）<br>· 地址记录 → `IPv4` / `IPv6`<br>· 记录头 → `TTL` |
| **出参** | `Host` 半成品（该地址的全部服务 + 主机级字段） |
| **失败** | 单条记录解析失败 → 跳过该记录，保留同一报文中的其他记录，**不丢弃整个报文** |
| **约束** | 本节点必须完整遵守 §7 的全部六条硬规则 |

#### 节点 ⑤ 聚合过滤

| | |
|---|---|
| **入参** | 全部 `Host` 半成品、端口范围 |
| **处理** | 1. **先用网段过滤组播通道的结果**，丢弃 `--cidr` 范围外的响应源<br>2. 合并两通道结果，同一 IP 的多条结果并为一个 `Host`<br>3. 服务按「类型 + 端口 + 实例名」去重<br>4. 按端口范围过滤 |
| **出参** | 最终 `Host` 列表 |
| **关键决策** | **无端口的服务不参与端口过滤，一律保留。**<br>理由：它压根没有端口，不归属任何端口区间。若一并过滤掉，`device-info` 这类承载型号信息的关键条目会被无故丢弃，直接损伤深度得分。 |
| **边界** | 同一服务在多个响应中重复出现时，保留信息最全的那条（描述记录非空优先） |

#### 节点 ⑥ 渲染输出

| | |
|---|---|
| **入参** | `Host` 列表 |
| **处理** | 按 4.4 的格式规则逐条渲染 |
| **出参** | stdout 文本 |
| **边界** | 结果为空时：stdout **不输出任何内容**，stderr 提示「未发现资产」，退出码仍为 `0` |

### 4.4 预期效果

#### 示例输入

```bash
./mdnsscan --cidr 192.168.1.0/24 --ports 1-10000
```

#### 示例输出

```text
services:
9/tcp workstation:
Name=slw-nas [24:5e:be:69:a3:13]
IPv4=192.168.1.20
IPv6=fe80::265e:beff:fe69:a313
Hostname=slw-nas.local
TTL=10
5000/tcp qdiscover:
Name=slw-nas
IPv4=192.168.1.20
IPv6=fe80::265e:beff:fe69:a313
Hostname=slw-nas.local
TTL=10
accessType=https,accessPort=86,model=TS-X64,displayModel=TS-464C,fwVer=5.2.9,fwBuildNum=20260214
device-info:
Name=slw-nas(AFP)
IPv4=192.168.1.20
IPv6=fe80::265e:beff:fe69:a313
Hostname=slw-nas.local
TTL=10
model=Xserve
answers:
PTR:
_workstation._tcp.local
_qdiscover._tcp.local
_device-info._tcp.local
```

#### 格式规则（逐条明确，避免实现时跑偏）

| 规则 | 说明 |
|---|---|
| 服务行 | `<端口>/<协议> <服务名>:` |
| **无端口服务** | 某些服务没有端口信息（如 `device-info`），此时服务行退化为 `<服务名>:`，**不能输出 `0/tcp`** |
| 属性行顺序 | `Name` → `IPv4` → `IPv6` → `Hostname` → `TTL` → 自报键值对 |
| 自报键值对 | **必须保持设备返回的原始顺序**，多个键值用 `,` 连接成一行 |
| 尾部段 | `answers:` / `PTR:` 后列出发现的全部服务类型 |

## 5. 架构设计

单向管线，六个阶段，每阶段职责单一。
**各节点的入参 / 出参契约见 §4.3，本节只描述代码结构。**


```
 ┌──────────────┐
 │ ① 入口       │  解析 --cidr / --ports / --timeout / --concurrency
 └──────┬───────┘
        ↓
 ┌──────────────┐
 │ ② 目标生成   │  CIDR → 地址列表（剔除网络地址与广播地址）
 └──────┬───────┘
        ↓
 ┌──────────────────────────────────┐
 │ ③ 并发探测（双通道并行）          │
 │  ├ ③a 组播：全网广播一次          │  捕获只听组播的设备
 │  └ ③b 单播：逐地址询问            │  worker pool，每地址独立超时
 └──────┬───────────────────────────┘
        ↓
 ┌──────────────┐
 │ ④ 解析       │  把回答拆成结构化记录
 │              │  服务类型 / 端口 / 主机名 / 地址 / 自报键值对
 └──────┬───────┘
        ↓
 ┌──────────────┐
 │ ⑤ 聚合过滤   │  同一设备的多条记录合并为一个 Host
 │              │  按端口范围过滤
 └──────┬───────┘
        ↓
 ┌──────────────┐
 │ ⑥ 输出       │  按 §4.4 格式渲染
 └──────────────┘
```

### 文件划分

扁平结构，全部在 `package main`，不建子包、不做接口抽象：

| 文件 | 职责 |
|---|---|
| `main.go` | 参数解析、管线编排 |
| `target.go` | CIDR 展开、端口范围解析 |
| `probe.go` | 并发探测、请求构造、超时控制 |
| `parse.go` | 响应解析 → 结构化记录 |
| `render.go` | 按格式输出 |
| `model.go` | 数据模型（§11-D4：原设计未列此文件） |

测试文件：

| 层次 | 文件 | 覆盖范围 |
|---|---|---|
| 单元 | `unit_test.go` | 纯函数：转义还原、名称拆解、排序、过滤、参数构建、渲染金标、记录索引 |
| 单元 | `target_test.go` | 网段展开边界、大网段内存、端口范围解析 |
| 组件 | `scan_test.go` | 多设备聚合、输出格式、端口与网段过滤、输出确定性 |
| 组件 | `probe_test.go` | 组播降级、取消传播、协程泄漏、并发上限、种子查询规则 |
| 集成 | `integration_test.go` | 经**真实 UDP 套接字**与假响应端验证全链路 |
| 端到端 | `e2e_test.go` | CLI 契约：进程内调用 `run()`，以及构建二进制后的**真实子进程** |

## 6. 数据模型

```go
type Host struct {
    IPv4     string
    IPv6     string
    Hostname string     // 设备自报的主机名
    Services []Service
    PTRTypes []string   // 用于 answers: PTR: 段
}

type Service struct {
    Name    string   // 实例名
    Type    string   // 服务类型：http / smb / qdiscover ...
    Proto   string   // tcp / udp
    Port    uint16   // 0 表示无端口（渲染时省略端口前缀）
    HasPort bool     // 显式标记，避免用 Port==0 判断产生歧义
    TTL     uint32
    TXT     []string // ★ 用切片不用 map：map 遍历顺序随机，会打乱输出
}
```

**设计要点**：`TXT` 字段是本项目的核心产出，必须原样保序透传，不做任何归一化或过滤。

## 7. 深度标识信息规格（核心）

本节定义本项目的核心产出。「深度标识」不是一个模糊的形容词，它可以被完整拆解成
**7 个信息来源 + 7 条输出字段 + 6 条处理规则**，下面逐条写死。

### 7.1 信息来源穷举

设备的一次回答里，能提取信息的位置只有以下 7 处，不多不少：

| # | 来源 | 提供什么 | 去向 |
|---|---|---|---|
| 1 | 服务类型清单 | 该设备对外声明的全部服务种类 | `answers: PTR:` 段 |
| 2 | 服务实例名 | 实例名本身，**常内嵌 MAC / 型号 / 别名** | `Name=` |
| 3 | 服务定位记录 | 端口 + 主机名 | 端口前缀、`Hostname=` |
| 4 | **服务描述记录** | **键值对形式的设备自述** | **深度 banner ★** |
| 5 | v4 地址记录 | IPv4 地址 | `IPv4=` |
| 6 | v6 地址记录 | IPv6 地址 | `IPv6=` |
| 7 | 各记录的存活时间 | TTL | `TTL=` |

**第 4 项是「深度」的主体。第 2 项是最容易被整条忽略的补充来源**——示例里
`Name=slw-nas [24:5e:be:69:a3:13]` 的那段 MAC 地址就藏在实例名里，不在描述记录中。

### 7.2 每条输出字段的精确来源与处理

| 输出 | 取自 | 处理规则 |
|---|---|---|
| `<端口>/<协议> <服务名>:` | 来源 3 的端口 + 服务类型 | **无定位记录时整个端口前缀省略**，只输出 `<服务名>:` |
| `Name=` | 来源 2 的第一段 | **必须做转义还原**，见 7.3-① |
| `IPv4=` | 来源 5 | 无则该行整行省略，不输出空值 |
| `IPv6=` | 来源 6 | 保留 `fe80::` 开头的链路本地地址；无则整行省略 |
| `Hostname=` | 来源 3 的目标名 | 去掉结尾的点 |
| `TTL=` | 来源 7 | 查询**源端口非 5353** 时设备会统一压成 10（与单播/组播无关，见 §8.2） |
| `k=v,k=v,…` | 来源 4 | 保序、原样、逗号连接，见 7.3 全部条款 |

### 7.3 六条硬规则（每条都对应一种会静默翻车的情形）

**① 转义还原——最容易翻车的一条**

实例名 `slw-nas [24:5e:be:69:a3:13]` 里含空格。它在协议里是一个完整的整体，
但绝大多数解析库把它转成字符串时会把空格写成 `\032`、把点写成 `\.`。

不还原的话输出会变成：

```text
Name=slw-nas\032[24:5e:be:69:a3:13]      ← 错，与示例对不上
Name=slw-nas [24:5e:be:69:a3:13]         ← 对
```

**必须实现的还原**：`\DDD`（三位十进制）与 `\X`（反斜杠加任意字符）两种通式。

> 实测修正：解析库对空格产出的是**反斜杠加空格**而非 `\032`。
> 通用还原器同时覆盖两种写法，故输出不受影响。

**② 保序——用切片，绝不用 map**

描述记录里键值对的顺序是设备定的，必须原样保留。
Go 的 map 遍历顺序随机，用 map 存会导致**每次运行输出顺序都不一样**，
且与示例对不上。这个错误不会报错、不会崩溃，只会静默失分。

**③ 只按第一个等号切分**

`accessType=https` 很简单，但值里可能本身含等号（如公钥 `pk=AbCd==`）。
切分时只切第一个 `=`，其余全部算作值。

**④ 无等号的项原样输出**

部分描述项是纯标志位，没有等号。直接原样输出，
**不要丢弃、不要自己补等号、不要跳过**。

**⑤ 空描述记录不产生空行**

描述记录可能是一条长度为 0 的空串，含义是「我没有额外信息」。
此时该服务不输出任何键值对行，**不能输出一个空行**。

**⑥ 零归一化**

不改大小写、不排序、不去重、不过滤不认识的键、不对值做任何解码。
**收到什么，原样输出什么。**

### 7.4 深度验收参照表

用于**判断深度够不够**，不作为实现依据（实现必须透传，见 7.5）：

| 设备类别 | 服务类型 | 预期能拿到的深度信息 |
|---|---|---|
| QNAP 存储 | `qdiscover` | 访问方式、管理端口、内部型号、展示型号、固件版本、固件构建号（**示例即此类**） |
| 打印机 | `ipp` / `ipps` | 型号名、厂商、产品标识、**管理界面 URL**、摆放位置备注、支持的文档格式、彩色/双面能力、设备唯一 ID |
| 扫描仪 | `uscan` / `uscans` | 型号、厂商、管理 URL、色彩空间、输入源、双面能力、协议版本 |
| Apple 主机 | `device-info` | 硬件型号（`Xserve` / `MacBookPro18,3`）、系统主版本号 |
| Apple TV / 音箱 | `airplay` / `raop` | 设备 MAC、硬件型号、系统版本、源版本、能力位、公钥 |
| 投屏设备 | `googlecast` | 型号名、用户自定义设备名、设备 ID、版本、能力掩码、**当前播放状态** |
| 智能家居 | `hap` | 型号、设备 ID、设备类别、**配对状态标志**、协议版本 |
| Linux / Samba 主机 | `workstation` | **MAC 内嵌在实例名中**，通常无描述记录 |
| 备份服务 | `adisk` | 备份卷名、卷标志、主机 MAC |
| 网页服务 | `http` / `https` | 访问路径（示例中的 `path=/`） |

> 具体键名随厂商与固件版本变化，**本表仅用于验收判断，不得写进代码**。

### 7.5 实现红线

| | |
|---|---|
| ❌ | 硬编码任何服务类型的键名白名单 |
| ❌ | 因为「不认识这个键」就丢弃 |
| ❌ | 对键值对排序、去重、改写 |
| ❌ | 用 map 存储键值对 |
| ✅ | 收到什么输出什么，顺序与内容逐字节不变 |

**唯一判据**：把描述记录拼出来的那一行，与设备原始返回**逐字节一致**。
做到这一点，深度自动达标；做不到，键名列得再全也没用。

> **唯一例外**：换行符、回车符与终端控制字符必须做**可见化转义**
> （如换行输出为字面的 `\n`），原因见 §9.5-S2。
> 转义必须**可逆且不得静默丢弃字符**——这是安全要求，不属于 §7.3-⑥ 所禁止的归一化。

## 8. 关键设计决策

### 8.1 端口范围是过滤器，不是扫描范围

设备的自我介绍里**自带端口号**。因此流程是：

```
先拿到全部服务 → 再用端口范围筛掉不需要的
```

而不是「遍历端口范围逐个探测」。后者既慢又发现不了东西，是本题最大的理解陷阱。

### 8.2 单播为主，组播兜底

逐地址单播询问是**主通道**：题目输入为网段，且能明确知道是哪个地址回答的，便于归属。

但纯单播有已知缺口——**不少精简实现（尤其 IoT 设备）只监听组播组地址，对直接单播询问不响应**，
纯单播会整类漏报。因此增加一条组播兜底通道，其结果再用网段过滤（见 §4.3 节点 ③a、⑤）。

> **一处需要澄清的误读**：示例输出中每条记录都是 `TTL=10`，
> 容易被当作「参考实现使用单播」的证据。
> 实际上 TTL 被压到 10 的触发条件是**查询的源端口不是 5353**，
> 与目标地址是单播还是组播**无关**。
> 因此 `TTL=10` 只能说明查询从临时端口发出，**不能**用来论证通道选择。

### 8.3 一次询问尽量多拿数据

设备回答时通常会把相关信息（端口、主机名、地址、自报键值对）
一并塞进回答的附加区。因此解析时应**优先榨干单次回答的全部内容**，
缺什么再补问，而不是机械地发起多轮往返。这是本项目最大的性能杠杆。

### 8.4 并发与超时

- worker pool 控制并发数（默认 **256**，硬上限 **1024**）
- 地址按需逐个产出并派发，不预先展开整个网段
- `--timeout` 是**单地址总预算**（默认 2s），覆盖 Q1–Q3 全部往返，**不是单次等待**
- 单地址失败不影响整体，静默跳过
- 组播通道与单播通道**并行**执行，耗时不串行叠加

**默认值的算术依据**：/24 有 254 个地址，并发 256 可**一批打完**，
整体耗时 ≈ 单地址预算 2s + 收尾余量，满足 C7 的 5 秒指标。

> 早期版本默认并发为 100，需分 3 批，仅超时一项就是 `3 × 2s = 6s`，
> 与 5 秒指标自相矛盾。默认值上调至 256 即为修正此处。

## 9. 验收标准

验收分六组。**9.1 是及格线，9.2 是得分点，9.3–9.6 是能不能算「工程完成」的分界线。**

### 9.1 基础功能验收

| # | 标准 | 判定方式 |
|---|---|---|
| F1 | 程序可编译可运行 | `go build ./...` 无错，`go vet ./...` 干净 |
| F2 | 参数生效 | 各参数按 §4.1 语义工作 |
| F3 | 输出含 IP | 每条资产有 IPv4 或 IPv6 |
| F4 | 输出含端口 | 有端口的服务正确显示 `<端口>/<协议>` |
| F5 | 输出含主机名 | `Hostname=` 字段非空 |
| F6 | **深度标识** | **通过 9.2 全部六项** |
| F7 | 端口过滤生效 | 缩小端口范围后，范围外的**有端口服务**消失；**无端口服务仍保留**（§4.3 节点⑤） |
| F8 | 格式一致 | 与 §4.4 示例逐行对齐，无端口服务不输出 `0/tcp` |
| F9 | `answers:` 段 | 列出发现的全部服务类型 |
| F10 | 退出码 | 正常 `0`（含无结果），参数非法 `2` |
| F11 | 双通道生效 | 组播通道能发现单播未覆盖的设备；组播不可用时自动降级为纯单播 |
| F12 | 种子查询 | 组播通道补发常见服务类型；单播通道不补发；已问过的类型不重复发送 |

### 9.2 深度标识专项验收（对应 §7.3 六条硬规则）

这六项全过才算深度达标，缺一项都会静默失分：

- [ ] **① 转义已还原**：`Name=` 行中不出现 `\032`、`\.`、`\\` 等转义序列，
      带空格的实例名显示为 `slw-nas [24:5e:be:69:a3:13]` 而非 `slw-nas\032[...]`
- [ ] **② 顺序稳定**：**连续运行三次**，同一设备的键值对顺序完全一致
      （顺序漂移即说明误用了 map）
- [ ] **③ 含等号的值未被截断**：形如 `pk=AbCd==` 的值完整保留
- [ ] **④ 无等号项未丢失**：纯标志位项原样出现在输出中
- [ ] **⑤ 空描述不产生空行**：无额外信息的服务，其条目下不出现多余空行
- [ ] **⑥ 零归一化**：大小写、重复键、未知键均未被改动或过滤

**总判据**：任取一条键值对行，与设备原始返回逐字节比对，必须完全一致。

### 9.3 边界条件验收

#### 输入边界

| # | 输入 | 预期行为 |
|---|---|---|
| B1 | `--cidr 192.168.1.5/32` | 只探测该单个地址，**不返回空** |
| B2 | `--cidr 192.168.1.0/31` | 探测 2 个地址，**不剔除网络/广播地址** |
| B3 | `--cidr 192.168.1.0/24` | 探测 254 个地址（剔除 `.0` 与 `.255`） |
| B4 | `--cidr 10.0.0.0/8` | **分批处理，不一次性展开进内存** |
| B5 | `--cidr 192.168.1.0/33` | 明确报错 + 退出码 2 |
| B6 | `--cidr abc` / 空值 | 明确报错 + 退出码 2 |
| B7 | `--cidr fe80::/64` | **明确支持或明确拒绝，不得静默返回空** |
| B8 | `--ports 80-80` | 单端口过滤生效 |
| B9 | `--ports 100-50` | 报错（下界 > 上界） |
| B10 | `--ports 0-100` | 报错（0 非合法端口） |
| B11 | `--ports 1-70000` | 报错（超出 65535） |
| B12 | `--ports abc` / 空值 | 报错 |
| B13 | `--timeout 0` 或负数 | 报错 |
| B14 | `--concurrency 0` 或负数 | 报错 |

**所有报错必须指明是哪个参数、为什么不合法，且一律不得 panic。**

#### 数据边界

| # | 场景 | 预期行为 |
|---|---|---|
| B15 | 描述记录为空 | 不输出键值对行，**且不输出空行** |
| B16 | 有实例名但无端口记录 | 输出 `<服务名>:`，**不输出 `0/tcp`** |
| B17 | 设备仅有 IPv6 | `IPv4=` 整行省略，**不输出 `IPv4=` 空值** |
| B18 | 实例名含中文 / emoji / 超长 | 正常输出，不乱码、不截断 |
| B19 | **描述记录的值里本身含逗号** | 原样输出（保真优先）。**已知会与分隔逗号产生歧义，记入 §10** |
| B20 | 响应超 MTU 被截断 | 解析已收到的部分，**不整包丢弃** |
| B21 | 同一服务在多个响应中重复 | 去重，保留信息最全的一条 |
| B22 | 组播收到**网段外**设备的回答 | **必须被网段过滤丢弃**，不得出现在输出中 |

### 9.4 异常与容错验收

| # | 场景 | 预期行为 |
|---|---|---|
| E1 | 全部地址无响应 | stdout 空，stderr 提示，**退出码仍为 0** |
| E2 | 目标返回端口不可达 | 静默跳过该地址，不中断整体 |
| E3 | 目标返回畸形 / 乱码报文 | 跳过该报文，**不 panic** |
| E4 | 单条记录解析失败 | **只跳过该条记录**，保留同一报文中的其他记录 |
| E5 | 网络接口不可用 | 启动时明确报错，**不得静默跑完再输出空**。套接字创建失败与「目标无响应」必须分开计数，全部失败时明确告警 |
| E6 | 无权限绑定端口 | 明确提示具体原因，**原始错误须原样带出**，使用者才能分辨权限、端口占用等不同成因 |
| E7 | 运行中 Ctrl-C | 5 秒内干净退出，无残留协程 |
| E8 | 目标响应极慢 | 按超时截断，不无限等待 |
| E9 | 组播套接字不可用 | **降级为纯单播**，stderr 提示，不中断整体 |

### 9.5 安全风险验收

**核心前提：设备自报的一切内容都是不可信输入。** 本工具的输入完全由被测网络中的任意设备控制。

| # | 风险 | 要求 | 验证方式 |
|---|---|---|---|
| **S1** | **响应源伪造** | UDP 无连接，任何主机都能伪造源地址投递响应。**必须校验响应源地址与询问目标一致，不一致一律丢弃**，否则测绘结果可被第三方投毒 | **由内核完成**：单播通道用面向连接的 UDP 套接字，内核只投递来自该目标地址的报文，无需用户态校验 |
| **S2** | **输出注入** | 实例名与描述值中的**换行符、回车符、ANSI 转义序列、终端控制字符**必须转义或剥离。否则设备可通过在描述记录里塞入换行来**伪造整段输出条目**，或用转义序列篡改终端显示 | 描述记录路径由解析库在解包时自动转义；名称路径由 `isSafeByte` 把守。`TestControlCharactersStayEscaped` 断言换行与 ANSI 序列均未被还原 |
| **S3** | **解析炸弹** | 名称压缩指针自指或成环时，解析不得陷入死循环或栈溢出 | `TestS3CompressionPointerLoops`：手工拼出自指与互指两种环，解析层与真实套接字双路验证 |
| **S4** | **内存放大** | 对单响应的记录条数与报文大小设硬上限，**不得按报文声明值预分配内存** | `TestS4*` 三个方向：声明 65535 条记录、灌包 400 条、投递 60 KB 巨型包 |
| **S5** | **资源耗尽** | 并发数设硬上限，文件描述符用量受控 | `maxConcurrency = 1024`，超出即截断并提示。`TestConcurrencyCap` 覆盖四种情形 |
| **S6** | **扫描授权** | README 明确声明**仅限在已授权网段使用** | 文档检查 |
| **S7** | **最小活动面** | 仅向 `--cidr` 指定范围发包，不主动出网、不上报任何数据 | `TestS7*`：记录全部 dial 目标并断言均在网段内、次数与网段展开一致；组播目标静态断言为链路本地组 |

> S1 与 S2 是本工具**最实际的两个风险**：前者让结果不可信，后者让攻击者能通过一台受控设备污染整份测绘报告。

### 9.6 并发与性能验收

| # | 项 | 要求 | 验证方式 |
|---|---|---|---|
| C1 | 无数据竞争 | 竞态检测器零告警 | `go run -race . --cidr <网段>` |
| C2 | 并发数受控 | 实际并发协程数不超过 `--concurrency` | `TestConcurrencyBound`：用阻塞桩替换套接字创建，使并发数可被静态观测，不依赖计时 |
| C3 | 无协程泄漏 | 结束后协程数回落至基线 | `TestNoGoroutineLeak`：轮询等待回落，避免调度抖动导致偶发失败 |
| C4 | 单点不拖垮整体 | 一个地址卡到超时，不影响其余地址按时完成 | `TestC4SlowTargetDoesNotBlockOthers`：正常目标与滞留目标同场，断言总耗时不超单目标预算的 1.6 倍 |
| C5 | 取消传播 | Ctrl-C 后全部协程在 5 秒内退出 | `TestCancellationPropagates`：预算设 30s，实测 0.15s 收尾 |
| **C6** | **服务顺序与示例一致** | 主机之间按 IP 排序；**同一主机内的服务保持服务类型清单的返回顺序，不得排序**。连续三次运行输出逐字节一致 | `diff` 比对三次运行结果 |
| C7 | 性能 | /24 网段 **5 秒内**返回 | 真实局域网计时；另有 `TestC7ConcurrencyDoesNotSerialize` 作为可复现门限：254 个黑洞目标实测 107ms，门限 2 秒 |

> **C6 的顺序规则直接来自示例**：示例 services 段的端口顺序是 `9 → 5000 → 445 → 5000 → (无) → 548`，
> **并未排序**；而它与 `answers: PTR:` 段的顺序**逐项一致**
> （workstation → http → smb → qdiscover → device-info → afpovertcp）。
> 因此同一主机内**必须保持清单返回顺序，任何排序都会打乱输出**。
>
> **C6 与 §7.3-② 是同一个坑的两个层面**：后者管单个服务内键值对的顺序，前者管主机与服务之间的顺序。
> 并发采集天然导致乱序，主机之间不按 IP 排序就无法复现。

### 9.7 验收环境与方法

macOS 自带设备发现服务，**本机即可作为被测目标**：

```bash
ipconfig getifaddr en0                      # 查本机网段
go run -race . --cidr <网段>/24 --ports 1-65535   # 带竞态检测跑一遍
```

局域网内若有 NAS、打印机、Apple TV、智能音箱，均会返回结果，
其中 NAS 与打印机通常携带型号、固件、管理地址，可直接验证 9.2 的深度要求。

**自动化测试**（分层与对应关系见 §9.9）：

```bash
go test -race ./...          # 全量，含竞态检测
go test -cover ./...         # 语句覆盖率
go test -run Integration ./... # 仅集成层
go test -run E2E ./...         # 仅端到端层
```

集成层会在本地临时端口起一个假响应端，端到端层会先构建二进制，
两者均不依赖真实局域网环境，可在任意机器上复现。

**三次一致性检查**（同时覆盖 9.2-② 与 9.6-C6）：

```bash
for i in 1 2 3; do ./mdnsscan --cidr <网段>/24 > /tmp/run$i.txt; done
diff /tmp/run1.txt /tmp/run2.txt && diff /tmp/run2.txt /tmp/run3.txt && echo "顺序稳定"
```

### 9.8 优先级与完成状态

| 优先级 | 内容 | 对应验收 | 状态 |
|---|---|---|---|
| **P0** | **单播**逐地址询问 → 解析 → 按格式输出（含描述记录） | 9.1 + 9.2 | ✅ 真实局域网实测通过 |
| **P0** | 并发 + 超时 | C7 | ✅ /24 实测 3.0 秒 |
| **P0** | **服务顺序与示例一致**（不排序，保持清单返回顺序） | **C6** | ✅ 含 200 次重复渲染断言 |
| P1 | **组播兜底通道** | F11、B22、E9 | ✅ 含降级路径双向验证 |
| P1 | 端口范围过滤、`answers: PTR:` 段 | F7、F9 | ✅ |
| P1 | 响应源校验、控制字符转义 | S1、S2 | ✅ 二者均为零成本达成，见 §9.5 |
| P1 | 参数校验与报错 | B5–B14 | ✅ 11 类输入 |
| P2 | 大网段按需生成 | B4 | ✅ 分配为常量级 |
| P2 | IPv6 地址补全 | B17 | ✅ |
| P3 | 内存放大压力验证 | S4 | ⬜ 仅设上限，未做压力测试 |
| P3 | 真实多设备环境 | F11 | ⬜ 仅合成数据验证 |

> **回顾：S1、S2 的时间预算判断偏保守。**
> 二者原被降到 P1 作为时间取舍，实现时发现都几乎零成本：
> S1 由面向连接的 UDP 套接字让内核代劳，S2 的描述记录路径由解析库
> 在解包时自动转义。真正需要动手的只是名称路径的一个字节门槛判断。

### 9.9 测试分层与验收项对应关系

语句覆盖率 **95.5%**，61 个用例、112 项断言。唯一未覆盖的是 `main`
函数体（仅一行 `os.Exit(run())`）。

#### 分层策略

| 层次 | 手段 | 回答的问题 |
|---|---|---|
| **单元** | 直接调用纯函数 | 单个函数的边界与异常输入处理是否正确 |
| **组件** | 在内存中构造已解包的消息 | 多个函数协作的逻辑是否正确 |
| **集成** | 本地假响应端 + 真实 UDP 套接字 | 收发、报文编解码、三段式问答是否真的可用 |
| **端到端** | 进程内 `run()` 与真实子进程 | CLI 契约、退出码、流分离是否符合约定 |
| **健壮性** | 注入故障、投递恶意报文、构造滞留目标 | 异常与攻击场景下是否仍然可靠 |

分层的意义在于定位：单元层失败指向某个函数，集成层失败指向网络
或协议处理，端到端层失败指向命令行契约。

#### 对应关系

| 层 | 测试 | 覆盖的验收项 |
|---|---|---|
| 单元 | `TestUnescapeName` | §7.3-①、§9.5-S2、§9.3-B18（15 条边界） |
| 单元 | `TestIsSafeByte` | §9.5-S2 与 §9.3-B18 的门槛判定 |
| 单元 | `TestSplitInstance` / `TestTypeAndProto` | 名称拆解的异常输入 |
| 单元 | `TestCompareIP` | C6 主机排序须为数值序 |
| 单元 | `TestFilterByPort` | F7 及无端口服务保留决策 |
| 单元 | `TestBuildConfig` | §4.3 节点①，1 组合法 + 8 组非法 |
| 单元 | `TestRenderHostGolden` | §4.4 格式规则，逐字节金标 |
| 单元 | `TestRenderHostOmitsMissingFields` | B17 空值行须整行省略 |
| 单元 | `TestRecordIndexScansAllSections` | §8.3 三个区段一视同仁 |
| 单元 | `TestInstancesMissingDetail` / `TestCollectPTRTargets` | Q3 补问对象的判定与去重 |
| 单元 | `TestExpandCIDRBoundaries` 等 4 项 | B1、B2、B3、B4、B7 |
| 单元 | `TestParsePortRange` | B8–B12 |
| 组件 | `TestMultiHost` / `TestOutputDetails` | 多设备聚合、F3–F5、F8、§7.3 各条 |
| 组件 | `TestPortFilterKeepsPortlessService` | §4.3 节点⑤关键决策 |
| 组件 | `TestCIDRFilterDropsOutOfRange` | B22 组播越界响应源过滤 |
| 组件 | `TestDeterministicOutput` | C6、§7.3-②（200 次重复渲染） |
| 组件 | `TestMulticastDegradation` 等 2 项 | E9 降级路径双向 |
| 组件 | `TestCancellationPropagates` | C5（预算 30s，实测 0.15s 收尾） |
| 组件 | `TestNoGoroutineLeak` | C3 |
| 组件 | `TestConcurrencyCap` / `TestConcurrencyBound` | S5、C2 |
| 组件 | `TestSeedTypesOnlyOnMulticast` 等 2 项 | F12 种子规则与去重 |
| **集成** | `TestIntegrationUnicastFullStack` | **F3–F6、F9**，经真实 UDP 核对 9 个输出字段 |
| **集成** | `TestIntegrationMulticastPath` | **F12**，确认种子查询真的发到线缆上 |
| **集成** | `TestIntegrationCollectsMultiplePackets` | §4.3 节点③b：不得收到首个报文即停 |
| **集成** | `TestIntegrationSilentTargetCostsLess` | 无响应地址早退优化确实生效 |
| **集成** | `TestIntegrationMalformedPacketIsSkipped` | **E3** 畸形报文跳过且不 panic |
| **端到端** | `TestE2EFullFlow` | **F1–F10 综合**，含 stdout 不被提示污染 |
| **端到端** | `TestE2EPortFilterEndToEnd` | F7 的命令行行为 |
| **端到端** | `TestE2EEmptyResult` | **E1** 空结果三要素 |
| **端到端** | `TestE2EParamErrors` | **B5–B14、F10**，10 种非法输入 |
| **端到端** | `TestE2EUsage` | 帮助信息完整性 |
| **端到端** | `TestE2EBinaryHelp` 等 5 项 | 真实进程的退出码、未知参数、流分离、不挂死 |
| **健壮性** | `TestE5NetworkUnusableIsReported` + 反向用例 | **E5** 网络不可用须明确告警，且有连通地址时不得误报 |
| **健壮性** | `TestE6PermissionDeniedSurfacesCause` | **E6** 降级提示须原样带出原始错误 |
| **健壮性** | `TestS3CompressionPointerLoops` | **S3** 自指与互指压缩指针环，解析层与套接字双路 |
| **健壮性** | `TestS4InflatedRecordCount` 等 3 项 | **S4** 声明膨胀、灌包截断、巨型包 |
| **健壮性** | `TestC4SlowTargetDoesNotBlockOthers` | **C4** 滞留目标不得拖垮其余地址 |
| **健壮性** | `TestC7ConcurrencyDoesNotSerialize` | **C7** 可复现性能门限（254 目标实测 107ms） |
| **健壮性** | `TestS7OnlyTargetsInsideCIDR` 等 2 项 | **S7** 全部 dial 目标落在网段内；组播目标为链路本地组 |
| 多设备 | `TestMultiDeviceConcurrentTargets` | 并发处理三个应答目标，经真实套接字取回全部深度标识 |
| 多设备 | `TestMultiDeviceDistinctSourceIPs` | **独立源地址**下的多设备聚合、排序与分块（需环回别名） |

#### 仍无自动化覆盖的部分

经测试补全，原先的 7 项空白已收敛至以下 3 项，均属环境固有限制：

| 项 | 现状与原因 |
|---|---|
| 独立源地址的多设备场景 | `TestMultiDeviceDistinctSourceIPs` 已写好且逻辑完整，但需要环回别名。**macOS 默认只分配 127.0.0.1，该用例会跳过并打印启用命令；Linux 默认整个 127/8 可用，会直接运行。** |
| 多台真实设备同时响应**广播** | 组播通道的代码路径已由集成测试覆盖（把组地址改指本地），但真实组播环境下多设备并发应答的行为无法在单机自动化中复现 |
| 真实网络的耗时 | `TestC7ConcurrencyDoesNotSerialize` 提供了与网络无关的可复现门限，但真实局域网耗时依赖环境，不能作为断言——只能手工计时 |

> 前两项的共同点是**需要真实的多主机网络**。
> 其余原本标注为「未覆盖」的条目（E5、E6、S3、S4、S7、C4、C7）
> 均已补上自动化验证，其中 E5 在补写测试时暴露为实现缺口（见 §11-X7）。

## 10. 已知取舍

- 组播通道只覆盖本地链路，跨网段的目标仍只能依赖单播通道
- 结果在内存中聚合，不做持久化；地址区间已改为按需生成，但聚合结果仍随发现的设备数增长
- 描述记录的值若本身含逗号，会与用作分隔的逗号产生歧义。
  为保持与示例格式一致且不破坏保真，选择原样输出，不做转义（见 §9.3-B19）
- 控制字符做可见化转义后，该字段与设备原始字节不再完全相同。
  这是 §9.5-S2 的安全要求与 §7.5 保真要求权衡后的结果，取安全优先

## 11. 实现偏差记录

实现过程中与本文档产生的全部偏差，均不改变预期输出。

| # | 条款 | 文档原定 | 实际实现 | 理由 |
|---|---|---|---|---|
| D1 | §7.3-① | 空格转义为 `\032` | 实测为反斜杠加空格 | 还原器按 `\DDD` 与 `\X` 两种通式实现，两种写法都能还原，输出一致 |
| D2 | §6 | `TXT []KV`（拆键值） | `TXT []string`（原样） | 输出只是逗号连接、从不按键查找；不拆分则 §7.3-③④ 自动成立，无法写错 |
| D3 | §9.5-S2 | 需自行转义控制字符 | TXT 路径由解析库自动完成 | 换行等控制字符在解包时已成字面 `\010`；名称路径由 `isSafeByte` 兜住 |
| D4 | §5 | 五个文件 | 增加 `model.go`，节点⑤置于 `main.go` | 数据模型独立成文件更清晰；聚合属管线编排 |
| D5 | §4.1 | `--ports` 仅 `a-b` | 额外支持单端口简写 | 纯增量，不改变既有行为 |
| D6 | §4.4 | 未规定多主机结构 | 每台设备一个完整区块 | 最贴近示例逐字格式 |

### 依赖版本的取舍

`miekg/dns` 最新版会把 `go.mod` 的 Go 版本要求顶到 1.25+，
将导致评审在较旧工具链上**无法编译**。
故锁定 `miekg/dns v1.1.61` 与 `x/net v0.26.0`，Go 版本要求压回 **1.22**。

### §9.3-B19 已在真实数据上应验

首台被测设备即出现含逗号的值：`model=Mac17,3`、`cn=0,1,2,3`、`et=0,3,5`。
含逗号的值是**常态而非例外**。题目示例本身也以逗号连接，
说明参考实现存在同样歧义，故维持原样输出的决定不变。

### 实现期发现并修复的缺陷

| # | 缺陷 | 后果 | 修复 |
|---|---|---|---|
| X1 | `parse.go` 的 PTR 类型兜底分支与 Q3 补问逻辑直接遍历 map | Go 的 map 遍历顺序随机，设备若不响应元查询，其服务顺序会在多次运行间漂移，违反 §9.6-C6 | 在 `recordIndex` 中增设 `ptrOwners` 切片记录首次出现顺序，所有遍历改走该切片 |

> X1 未被首轮实测发现，是因为受测设备正常响应了元查询，兜底分支从未执行。
> 该缺陷由「多设备是否可用」的复查引出，并已由 `TestDeterministicOutput`
> （200 次重复渲染比对）固化，防止回归。

### 打磨阶段的后续改动

| # | 改动 | 对应条款 | 说明 |
|---|---|---|---|
| X2 | 取消信号贯通至阻塞中的 UDP 读 | §9.6-C5 | 此前 ctx 只在派发处检查，协程进入阻塞读后只能等读超时；`--timeout 30s` 时 Ctrl-C 要干等 30 秒。改用 `context.AfterFunc` 在取消时关闭连接，实测 0.15 秒收尾 |
| X3 | 并发数硬上限 1024 | §9.5-S5 | 此前并发完全由用户指定，大网段配合高并发会耗尽 fd；而套接字创建失败是静默跳过的，表现为「扫不到东西」而非报错，极难排查 |
| X4 | 网段改为按需生成 | §9.3-B4 | 此前返回 `[]net.IP`，实测 /12 占 29.4 MB，/8 推算约 470 MB。改为 8 字节的 `ipRange` 值类型 + 流式派发 |
| X5 | 广播通道补发种子服务类型 | §2 目标 | 不响应元查询的设备此前会整台漏报。补发 22 个常见类型；仅在广播通道补发以控制开销 |
| X6 | 完善帮助信息 | — | 在 `--help` 中明确点出「端口范围是过滤器而非扫描范围」 |

### 为可测性所做的结构让步

三处套接字与输出的创建被提取为变量或参数，唯一目的是让难以构造的
路径变得可断言。这偏离了「不做接口抽象」的简洁原则，但换来的是
以下路径从「只有实现」变成「有验证」：

| 提取项 | 换来的验证能力 |
|---|---|
| `listenUDP` | §9.4-E9 广播套接字创建失败后的降级路径 |
| `dialUDP` | §9.6-C2 实际并发数的精确测量（阻塞桩，不依赖计时） |
| `probeAll` 的 `warn` 参数 | 降级与截断提示确实发出，而非静默 |

### §10 补充：单播通道的发现率取舍

单播通道在元查询无响应时即放弃该地址，不再补发具体服务类型查询。

理由：单播按地址逐个发送，若每个地址都补发 22 个查询，
/16 网段的查询量会达到百万级，且死地址的耗时会从预算的 1/3 涨到全额。
此类设备改由广播通道兜底（见 X5）。代价是跨网段、不响应元查询的
设备仍会漏报——广播只覆盖本地链路。

### 测试补全阶段发现的缺陷

| # | 缺陷 | 后果 | 修复 |
|---|---|---|---|
| X7 | 套接字创建失败与「目标无响应」共用同一条静默跳过路径 | 网络接口断开或路由不通时，程序照常跑完并输出「未发现资产」，把**环境故障伪装成扫描结果**——使用者会误判网段里确实没有设备。违反 §9.4-E5 | `probeUnicast` 返回 error 专指套接字创建失败；`probeAll` 分别计数，全部失败时明确告警并点明这与「未发现资产」不是一回事 |
| X8 | `model.go` 的 `hostKey` 方法从未被调用 | 死代码 | 移除 |

> X7 是补写 E5 测试时才暴露的——原先该项只标注为「未构造」，
> 实际是**实现缺口而非测试缺口**。这说明「未覆盖」的条目里
> 可能藏着真问题，不能假定未测试的代码是对的。
